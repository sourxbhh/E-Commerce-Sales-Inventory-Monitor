# Analytics deps layered onto the Airflow image so DAG tasks can run the
# pipeline in-process. Versions match the repo-root requirements.txt.
# (Airflow itself + providers come from the base image's constraints.)
duckdb==1.1.3
# Pin the whole dbt dep set to the versions proven on the local box so the image
# resolves an identical, reproducible dbt stack (not just whatever pip picks).
dbt-core==1.8.9
dbt-duckdb==1.8.4
dbt-adapters==1.16.3
dbt-common==1.27.1
dbt-semantic-interfaces==0.5.1
dbt-extractor==0.6.0
dbt-protos==1.0.514
mashumaro==3.14
great-expectations==1.18.0
pyarrow==17.0.0
pandas>=2.1,<2.3
pydantic-settings==2.6.1
python-dotenv==1.0.1
